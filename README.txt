FantEO landing page - image-matched HTML version

Open index.html in your browser.

This version uses the approved generated landing-page image as the visual background, so it looks identical to the mock-up. The email button and fanteo.space text are clickable transparent links over the image.

To publish on Cloudflare Pages, upload the whole folder and keep this structure:

index.html
assets/fanteo-landing-background.png
